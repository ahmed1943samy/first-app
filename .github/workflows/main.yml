
on:
  push:
    branches:
      - master




jobs:
  bulid: 
    runs-on: ubuntu-latest
    
    
    steps:
      - uses: actions/checkout@v3
      
      - name: "Login to Docker Hub"
        run: docker login --username ${{ ahmed1943samy }} --password ${{ AhmedSamy194112@@ }}


      - name: "docker image"
        run: docker build -t ${{ ahmed1943samy }}/myimage .

      - name: "docker push"
        run: docker push ${{ ahmed1943samy }}/myimage

